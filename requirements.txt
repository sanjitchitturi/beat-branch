# requirements.txt for BeatBranch
# Core dependencies for feature extraction, ML, and utilities
numpy>=1.21,<2.0
pandas>=1.3,<2.0
scipy>=1.7
scikit-learn>=1.0,<2.0
librosa>=0.9.2
soundfile>=0.12
joblib>=1.2
matplotlib>=3.4
tqdm>=4.60
python-dotenv>=0.21

audioread>=2.1.9

# Optional (uncomment / include if you plan to use these features)
# Web demo / lightweight UI
# streamlit>=1.18
# flask>=2.0
# fastapi>=0.95
# uvicorn>=0.18

# Deep-learning / audio embeddings (only if you plan to use PyTorch/TensorFlow)
# torch>=1.13
# torchaudio>=0.13
# tensorflow>=2.9

# Helpful extras
# pretty_midi>=0.2.10  # MIDI utilities, if using MIDI features
# librosa.display is part of librosa; seaborn can be used for plotting (optional)
# seaborn>=0.11

# Notes:
# - These use permissive lower bounds to avoid pinning to exact versions. For
#   reproducible experiments, create and commit a pinned file using:
#     pip freeze > requirements-pinned.txt
# - Install with: pip install -r requirements.txt
# - If you need a conda environment, prefer declaring versions in environment.yml
